<% tracks.forEach(e => { %>
<div class="track-item" data-id="<%= e.track_id %>" data-path="<%= e.path %>" data-artist="<%= e.artist %>">
  <p class="track-title"><%= e.track_title %></p>
  <p class="track-duration">3:32</p>
</div>
<% }); %>